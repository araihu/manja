# Manja Site

This module serves the Manja product site.

```bash
go run ./cmd/server
go test ./...
```

Routes:

- `/` product landing page
- `/demo` public OpenAPI renderer demo
- `/docs` setup and publishing docs
