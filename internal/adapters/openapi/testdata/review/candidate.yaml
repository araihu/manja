openapi: 3.1.0
info:
  title: Payments API
  version: 1.1.0
paths:
  /payments:
    get:
      parameters:
        - name: account_id
          in: query
          required: true
          schema:
            type: string
      responses:
        "202":
          description: Accepted
components:
  schemas:
    Payment:
      type: object
      properties:
        id:
          type: string
