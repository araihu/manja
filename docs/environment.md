# Environment Variables

## Config

 - `MANJA_RESOURCE_LIMITS` (default: `false`) - Enforce Manja's conservative catalog source, compilation, startup, storage, and HTML rendering budgets.
 - `MANJA_LOCAL_DOCS` (default: `on`) - Enable or disable local docs in the recovery-only runtime. Accepted values are on and off.
