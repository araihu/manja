# Goshtoso v0.0.12 Consumer Migration Snags

Date: 2026-07-25
Status: Final author gate complete on 2026-07-26; ready for independent review
on a local, unpushed branch

## Execution Baseline

The migration worktree is
`/Users/guilhermecastro/.codex/worktrees/4310/manja` on
`codex/goshtoso-v0.0.12-migration`. It was created by the Codex app, verified
clean, and attached to the task branch at exact `origin/main`
`6216bcb2d9a50083b130daa37cf381c6e2b4b601`. The primary checkout, the older
`codex/release-tracks-previews` worktree, and every other worktree were left
untouched.

The recovery close used the separate app-created worktree
`/Users/guilhermecastro/.codex/worktrees/134e/manja`. It verified clean and
detached at the preserved migration checkpoint
`3d0143f9f3b28e4ee8ed5132f9e0d8cdbefc19b3`, while the preserved
`codex/goshtoso-v0.0.12-migration` branch still pointed to that same commit.
After fetching, `origin/main` and the merge-base were both
`6216bcb2d9a50083b130daa37cf381c6e2b4b601`. Recovery created only
`codex/goshtoso-v0.0.12-migration-close` at the exact checkpoint; it did not
move the preserved branch or mutate another worktree.

Before the dependency change:

- root and `site/` both resolved `github.com/araihu/goshtoso v0.0.10` with no
  replacement; `site/` held it indirectly;
- root and `site/` both declared Go 1.26.1, while the installed Go command was
  `go1.26.1 darwin/arm64` with automatic toolchain selection available;
- root tests (including `internal/web/e2e`), `site/` tests, root/site builds,
  14 frontend example tests, API bundling, and templ generation all passed;
- templ generation reported `updates=0`, and the worktree remained clean;
- API lint passed with the three pre-existing warnings for missing `info.license`,
  the localhost server URL, and the health operation's missing 4XX response;
- `npm ci` completed but reported pre-existing deprecation notices and 22 audit
  findings (1 low, 12 moderate, 9 high).

## Consumer Skill Example History

The installed `using-goshtoso` skill demonstrated this removed call during the
planning inspection:

```go
button.Button(button.Config{Variant: button.Primary})
```

Goshtoso v0.0.12 removes `button.Config`, the generic `Variant` vocabulary,
and compatibility aliases. The tagged migration guide is authoritative:
Manja must use `button.Button(options ...button.Option)` with package-owned
options such as `button.WithTone(button.TonePrimary)`.

The installed skill was refreshed before implementation and now demonstrates
`button.Button(button.WithTone(button.TonePrimary), ...)`. The planning-time
source dive remains relevant historical friction, but the current skill example
is no longer stale. Its asset, `head.Dependencies()`, templ-generation, and
consumer-boundary guidance remains applicable; the tagged v0.0.12 migration
guide is authoritative for this task.

## Removed Renderer Helpers

Before migration, Manja called:

- `badge.Config.SoftVariantClasses`
- `table.BadgeCellClasses`

Both are removed/private renderer internals in v0.0.12. Resolving the migration
required reading the tagged migration guide and
`components/table/types.go`/`table.templ`. The supported path is to compose
`badge.Badge` directly and, inside a table, assign it to
`table.Cell.Component`.

The migration changed table-cell construction and rendered behavior tests
without recreating either class assembler in Manja.

## Go Toolchain Floor

`go mod download github.com/araihu/goshtoso@v0.0.12` reports that the module
requires Go 1.26.5 or newer. The previous Manja root and `site/` declarations
were Go 1.26.1. Both final module declarations are Go 1.26.5, the recovery
toolchain reports `go1.26.5`, and both final graphs resolve exactly
`github.com/araihu/goshtoso v0.0.12` with no replacement. The previous
resolved dependency was v0.0.10 in both modules.

## Missing Intermediate Changelog Entry

Manja currently resolves Goshtoso v0.0.10, while the v0.0.12 migration guide
uses v0.0.11 as its exact base. The tagged v0.0.11 module tree does not contain
a `CHANGELOG.md` entry.

Impact: execution must inspect the official v0.0.10-to-v0.0.11 comparison and
record the absence rather than inventing intermediate release notes.

Execution confirmed the tag has no `CHANGELOG.md`. The official comparison is
four commits and 59 changed files. The only net component-library implementation
change is the segmented-radio focus/scroll fix: the radio input changed from an
offscreen `sr-only` control to a transparent full-segment overlay, with matching
tests and generated output. The remaining net changes are documentation, demo,
generated demo output, CI, release-checklist, SEO, and consumer-skill work.
Because Manja uses segmented public/private radios, keyboard focus and unwanted
document scrolling are included in browser verification. No intermediate
release notes are inferred from commit messages.

## Live Consumer Inventory

The only Go modules in scope that resolve Goshtoso are the root module and
`site/`; `integration/testdata/external-module` does not consume it. The root
imports `assets` plus these component packages:

- shell/assets: `assets`, `head`;
- public docs: `accordion`, `badge`, `breadcrumbs`, `codeblock`, `schemaform`,
  `search`, `select`, `sidebar`, `table`, and `tabs`;
- management: `badge`, `button`, `radio`, `select`, `sidebar`, `table`, `tabs`,
  `textinput`, and `toggle`.

The mechanical checklist found these legitimate source-owned migrations:

- `badge.Config.Variant`, `badge.Config.Style`, old badge constants,
  `badge.Variant` helper return types, and `badge.Config.SoftVariantClasses`;
- three `button.Config` constructor calls using Primary or Alternate tone;
- two segmented `radio.Config.Variant` calls;
- one `toggle.Config.Variant` plus `Style` call;
- one `accordion.AccordionConfig.Variant` call;
- seven `table.BadgeCellClasses` calls.

Hits in generated `*_templ.go` files duplicate `.templ` sources. `ReadOnly` in
the bundled schema-example JavaScript is OpenAPI sampling behavior, not
`rating.Config.ReadOnly`. Raw `"default"` hits belong to Manja domain values,
OpenAPI fixtures, or generated JavaScript; there is no raw Goshtoso
Appearance/Mode persistence or comparison. No removed table/combobox fragments,
split primitives, Link/Kbd/Tooltip configs, Carousel, Modal/AlertDialog,
Rating/RatingDisplay, Toast/MessageToast, Banner/CookieBanner, or exact
constructor function types are present.

## Tagged Sources Consulted

Implementation read the complete tagged `CHANGELOG.md`,
`docs/MIGRATING_COMPONENT_API.md`, and `docs/COMPONENT_MODEL.md`; the full
generated component reference; the pkg.go.dev v0.0.12 module/package index; the
official v0.0.10-to-v0.0.11 compare metadata and non-generated radio source/test
diff; and the tagged v0.0.12 `components/badge`, `components/button`,
`components/radio`, `components/toggle`, `components/accordion`, and
`components/table` public types/templates needed by real Manja call sites.

The source dive confirms the supported replacement for removed table-cell
classes is `table.Cell.Component` composed with the concrete
`badge.Badge(...)` result. One narrower public API gap remains:
`sidebar.Item` exposes only `Badge string` plus the documented
`BadgeClass string` override, with no component slot. Manja's method badges
therefore cannot be replaced by `badge.Badge(...)` without changing Goshtoso.
The approved plan is amended to use a sidebar-only semantic-token mapping for
GET/POST/PUT/PATCH/DELETE while the sidebar continues to own layout, sizing,
and alignment. This is a recorded CSS escape hatch, not a copy of the removed
`SoftVariantClasses` or `BadgeCellClasses` assemblers. No Goshtoso library
change, compatibility alias, or compatibility fork is proposed.

## Scope Decision

No Goshtoso library change, compatibility fork, copied helper, release-track
work, preview behavior, licensing claim, or UI redesign belongs in this task.
The dependency bump and compatibility changes remain isolated after the Open
Core checkpoint and before the separate UI-refactor and release-track work.

## Component Migration Checkpoint

The consumer API RED gate failed against both templates on generic
`Variant`/`Style`, `button.Config`, `badge.Variant`,
`SoftVariantClasses`, and `BadgeCellClasses`. After the minimal migration it
passes. Manja now uses package-owned `Tone` and `Appearance` dimensions,
functional button options, `badge.Badge` composition for status content, and
`table.Cell.Component` for table-owned badges. The segmented radios retain the
v0.0.11 full-segment transparent input behavior, and the request accordion
retains `AllowMultiple: true` with its standard appearance.

The complete post-migration mechanical checklist was rerun. Remaining old-API
terms occur only in this migration ledger, the approved plan, and the
architecture regression patterns. `ReadOnly` remains OpenAPI sampler behavior;
raw `"default"` hits remain Manja IDs/domain defaults, generated JavaScript, or
the GitHub OpenAPI fixture. There are no legitimate removed methods,
constructors, fragments, split-primitives, exact constructor function types,
or raw Goshtoso appearance/mode comparisons in consumer source.

Official templ generation updated `management_templ.go` and `public_templ.go`;
a second generation reported zero updates. Focused architecture, root web,
Playwright E2E, and site tests passed. Beyond the documented sidebar
`BadgeClass` gap, this slice found no additional missing API, generation
slowdown, library change need, or workaround.

## Real Call-Site Migration

| Previous Manja use | Final v0.0.12 use |
| --- | --- |
| generic badge `Variant` and `Style` fields/constants | package-owned `badge.Tone` and `badge.Appearance` |
| `button.Config` plus old constants | `button.Option` values such as `WithTone`, `WithType`, and `WithRootClass` |
| radio/toggle generic `Variant` and toggle `Style` | package-owned `Tone` and `Appearance` fields |
| accordion generic `Variant` | `accordion.Appearance` |
| `table.BadgeCellClasses` | concrete `badge.Badge(...)` assigned to `table.Cell.Component` |
| `badge.Config.SoftVariantClasses` in sidebar items | narrow semantic-token classes through the documented `sidebar.Item.BadgeClass` escape hatch |

## Browser Matrix And Interactions

The previous runtime completed the literal matrix at the same exact candidate
commit before it was interrupted. The matrix was:

- surfaces: public endpoint detail and management workbench;
- viewports: 390 px and 1440 px;
- themes: Manja, Goshtoso, and Minimal;
- color modes: light and dark.

That is `2 × 2 × 3 × 2 = 24` representative states. All 24 passed with no
horizontal overflow, no console warning or error, and no failed asset or
navigation request. The pass also covered direct endpoint loads, composed
status and table badges, management buttons, segmented public/private radios,
the sync-publication toggle, request-config accordion multi-open behavior,
Ctrl/Cmd+K search-result navigation, HTMX schema navigation, and browser
Back/Forward history. It therefore preserves the direct, HTMX, history,
search, and affected-control evidence required by this compatibility slice.

## Official Air Rebuild And HTTP Evidence

Recovery ran `npm run dev` from its own worktree. The launcher selected app
`http://127.0.0.1:8288`, Air proxy `http://127.0.0.1:7539`, and product site
`http://127.0.0.1:8388`. The initial build settled with templ `updates=0`; both
the product-site and Air-proxy URLs returned HTTP 200.

A metadata-only `touch` advanced the `.templ` mtime but was not surfaced as a
write event by Air on this macOS host. A byte-identical patch of the same
`internal/web/templates/public.templ` source then emitted the required write
event while `git diff` continued to prove identical content. Air reported
exactly one `public.templ has changed` event and one `dev:build`; templ again
reported `updates=0`, and the rebuilt binary mtime advanced from 14:45:47 to
14:48:23 local time. A subsequent 15-second idle observation produced no
additional build output, proving generated writes did not create a loop. Both
URLs still returned HTTP 200 while idle, and the source had no content drift.
Recovery then interrupted only its own launcher session and proved its app,
proxy, and site ports had no remaining listeners.

The mtime-only event behavior is host/runtime friction, not a Goshtoso defect
or a repository code regression.

## Fresh Final Automated Gate

The recovery close reran the final gate from the exact candidate plus this
ledger update:

- `go run github.com/a-h/templ/cmd/templ generate` twice: PASS, `updates=0` on
  both passes;
- `npm run examples:build`, `npm run css:build --if-present`, and
  `npm run api:bundle`: PASS;
- root `go test ./... -count=1`, `go test -race ./... -count=1`, `go vet ./...`,
  `go build ./...`, and `GOWORK=off go mod tidy -diff`: PASS;
- the same test, race, vet, build, and tidy-diff gates from `site/` with
  `GOWORK=off`: PASS;
- `npm run examples:test`: PASS, 14 of 14 tests;
- `npm run api:lint`: PASS with only the three baseline warnings for missing
  `info.license`, the localhost server URL, and the health operation's missing
  4XX response;
- the dependency/component architecture gates: PASS;
- the tagged migration guide's dimension, removed-config, split-primitive,
  renderer-internal, complete removed-method, and concrete-constructor scans:
  no hits in consumer production source;
- the raw-default scan found only `id = "default"` in `public.templ`, a Manja
  request-example identifier rather than a raw Goshtoso Appearance/Mode
  comparison;
- root/site final dependency inventory: v0.0.12, no replacement, dependency
  `GoVersion` 1.26.5; root/site module directives and active toolchain are also
  1.26.5;
- `git diff --check origin/main...HEAD`: PASS.

`npm ci` still reports the baseline deprecation notices and 22 audit findings
(1 low, 12 moderate, 9 high). Redocly also prints its available-update notice.
Neither is introduced by the migration. API lint remains valid with exactly
the three warnings recorded above.

## Recovery And Runtime Friction

The original migration task had two interrupted runtime turns before it could
write the Air evidence, final ledger, or completion callback. Its three commits,
clean worktree state, automated results, and literal browser commentary remained
readable, so recovery did not need to infer or recreate the 24-state result and
did not lose source changes. This is control-plane/runtime lifecycle friction;
it is not a Goshtoso library defect. No missing archived rollout was required
for recovery because the task record was still available through the control
plane.

No new consumer implementation, UI refactor, release-track behavior,
authenticated preview, licensing change, try-it console, or upstream proxy was
added during recovery. The only remaining author-visible caveats are the
documented public sidebar `BadgeClass` escape hatch and the pre-existing npm and
API-lint warnings. The migration is ready for independent review.
